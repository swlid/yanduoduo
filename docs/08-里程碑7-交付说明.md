# 里程碑 7 交付说明（测试、打磨与上线准备）

> 状态：后端自动化测试、Docker 打包与上线配置已完成。
> 日期：2026-09-07

## 本里程碑做了什么

1. 建立 `backend/tests/` 测试目录与独立测试数据库。
2. 编写 7 个端到端冒烟测试，覆盖：
   - 健康检查。
   - 院校专业组合筛选。
   - 详情与横向对比。
   - 择校测评与三梯度推荐。
   - 计划生成、打卡与进度。
   - 时间线与节点订阅。
   - 后台资讯、用户纠错与审核。
3. 新增 `backend/Dockerfile` 与 `backend/.dockerignore`。
4. 更新 `docker-compose.yml`，加入 API 服务并与 PostgreSQL、Redis 联动。
5. 新增 `backend/requirements-dev.txt`。

## 验证结果

```powershell
cd backend
..\.venv\Scripts\python.exe -m pytest -q
```

结果：`7 passed`。

## 上线准备说明

- 本地开发可使用 SQLite；生产通过 `DATABASE_URL` 连接 PostgreSQL。
- `docker compose up --build` 可启动 API、PostgreSQL、Redis。
- 管理后台网页由 API 同一服务提供。

## 仍待人工完成的事项

- 微信开发者工具中导入小程序前端并完成页面联调。
- 接入微信登录与订阅消息。
- 配置真实域名、HTTPS 和管理员鉴权。
- 真实数据批量导入与人工审核。

## 下一步建议

- 先把当前变更提交为初始 commit。
- 在小程序开发者工具中做一次全流程人工验收。
