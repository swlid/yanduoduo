# 院校库扩充说明（全国招生单位 885 所）

> 日期：2026-09-17
> 状态：已完成导入并验收。院校库由 20 所扩充为 **885 所**，自划线 34 所。

## 1. 数据来源

用户投递的 `data/imports/考研数据汇总_2026-09-17.xlsx` 中「02_招生单位885所」表，提取件为 `data/imports/extracted/02_招生单位885所.csv`。原表字段含研招网 schId、校名、省份、主管部门、是否双一流、是否有研究生院、是否自划线、研招网学校页链接。来源与质量核验详见 `data/imports/README.md`。

层次标注使用 `data/imports/extracted/ref_levels.json`：985/211 取公开的 211 工程院校名单（官方口径 112 所，计入地矿油分家为 115 所），双一流取原表标记，两者交叉校验一致（115 + 33 = 148，等于原表标注的双一流总数）。

## 2. 覆盖结果

| 维度 | 结果 |
| --- | --- |
| 院校总数 | 885（导入前 20） |
| 层次 | 985 共 39 所、211（非 985）76 所、双一流（非 211）33 所、普通 737 所 |
| 自划线 | 34 所，与公认 34 所自主划线高校逐一对齐 |
| 省份 | 31 个省级行政区，最多的为北京 146、江苏 53、陕西 51、上海 43、湖北 43 |

## 3. 字段映射与口径

| 库字段 | 取值方式 |
| --- | --- |
| `code` | 原有 20 所保留 5 位单位代码；新增院校使用研招网 schId |
| `name` / `province` | 原表校名 / 省份 |
| `city` | 「暂无」（本批次不含城市，不推测） |
| `level` | `ref_levels.json` 的 985 / 211 / 双一流 / 普通 |
| `category` | 「暂无」（本批次不含院校类型，按用户确认填「暂无」） |
| `is_self_draw` | 原表 `is_self_line` |
| `official_url` | 研招网学校页链接 |
| `tags` | `yz_sch_id`、`department`、`is_double_first`、`has_graduate_school`、`code_type` |

约定：只录入原表可核字段，不推测城市、院校类型与单位代码。新增院校的 `code` 是研招网 schId（非 5 位单位代码），已在 `tags.code_type` 标记为 `yz_sch_id`，取得单位代码后可整批覆盖；原有院校的 `code_type` 为 `unit_code`。脚本幂等，重复执行只做更新。

## 4. 验证结果

- 重复校名 0 条、重复 `code` 0 条；885 行全部落库。
- 34 所自划线标记与公认名单完全一致（无多无缺）；39 所 985 全部命中且均标双一流。
- 原有 20 所的单位代码、城市、院校类型均保留，未被覆盖。
- 院校-专业组合仍为 132 个、招录记录仍为 372 条，本次扩充未触碰分数线数据。
- 后端 `pytest` 7 项通过；全流程验收脚本 13 项全部 PASS。
- 接口核对：`/api/v1/institutions` 返回 total = 885。

## 5. 运行方式

```powershell
# 1) 初始化库（如尚未初始化）
.venv\Scripts\python.exe backend\app\seed.py
# 2) 导入院校骨干（本批次）
.venv\Scripts\python.exe scripts\ingest_institutions.py
# 3) 导入官方招录数据与国家线（既有批次）
.venv\Scripts\python.exe scripts\ingest_official_batch.py
.venv\Scripts\python.exe scripts\ingest_national_lines.py
```

## 6. 已知限制与后续

- 城市（865 所）、院校类型（865 所）、5 位单位代码（865 所）仍为空缺，分别显示「暂无」，待后续数据源补齐。
- 新增院校目前只有院校信息、没有分数线与招生计划数据，因此不会出现在以「院校-专业组合」为主体的查询列表中；后续需补充分数线，或为小程序增加院校浏览入口。
- `data/official/national-lines.csv` 已覆盖 2024-2026 国家线 A/B 类，可作为尚未录入校线院校的基准参考。
